# Data Source

The real source file is the UCI Online Retail II CSV mirror used by the build script.

The script copies the source into this workspace when available and then generates the cleaned outputs.
